use_frameworks!

def shared_pods
  pod 'Alamofire', '~> 4.0'
  pod 'SwiftyJSON'
end

target 'OrlandoParking' do
  platform :ios, '9.0'
  shared_pods
end

target 'OrlandoParkingWatch Extension' do
  platform :watchos, '2.0'
  shared_pods
end
